import { Router } from 'express'

const router = Router()

// This file is kept to avoid breaking imports, but the test-token route
// has been removed as token generation is now handled by Clerk.

export default router
