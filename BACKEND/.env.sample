# Environment: 'development' or 'production'
NODE_ENV=development

# Your Google AI Studio API Key
GEMINI_API_KEY="your_api_key_here"

# The port for the Express server (optional, defaults to 3000)
PORT=3000

# The public URL of your deployed application (for production health checks)
# For local dev, this can be the same as your localhost.
# On Render, this would be your service URL, e.g., https://your-app-name.onrender.com
API_URL=http://localhost:3000
