name: CI

# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     strategy:
#       matrix:
#         node-version: [18.x]

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3

#       - name: Use Node.js ${{ matrix.node-version }}
#         uses: actions/setup-node@v3
#         with:
#           node-version: ${{ matrix.node-version }}
#           cache: 'npm'

#       - name: Install dependencies
#         run: npm ci

#       - name: Lint code
#         run: npm run lint

#       - name: Check formatting
#         run: npm run fmt:check

#       - name: Run tests
#         run: npm test

#       - name: Build project
#         run: npm run build

#     # Optional: Add step to build and push Docker image
#     # - name: Build and push Docker image
#     #   uses: docker/build-push-action@v2
#     #   with:
#     #     context: .
#     #     push: true
#     #     tags: your-docker-hub-username/content-service:latest
